import { MarketingConfig } from "types"
import { pages } from "./pages"

export const marketingConfig: MarketingConfig = {
  mainNav: [pages.features, pages.pricing, pages.blog, pages.documentation],
}
